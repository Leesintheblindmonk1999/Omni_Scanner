# .env.example — Omni-Scanner Semantic v2.0
# Copy this file to .env and fill in your values.
# NEVER commit .env to the repository.

# ── OpenRouter API (optional — for LLM agent) ─────────────────
OPENROUTER_API_KEY=your_openrouter_key_here

# ── Webhook (optional — for audit notifications) ──────────────
WEBHOOK_URL=

# ── Ollama (optional — local LLM, no key needed) ──────────────
# Default: http://localhost:11434
# Override only if running Ollama on a different host/port:
# OLLAMA_HOST=http://localhost:11434

# ── Streamlit (optional) ──────────────────────────────────────
# STREAMLIT_SERVER_PORT=8501
# STREAMLIT_SERVER_HEADLESS=true
